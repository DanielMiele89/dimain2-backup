CREATE TABLE [MI].[SchemeDateParams] (
    [ID]        TINYINT NOT NULL,
    [StartDate] DATE    NOT NULL,
    [EndDate]   DATE    NOT NULL,
    CONSTRAINT [PK_MI_SchemeDateParams] PRIMARY KEY CLUSTERED ([ID] ASC)
);

