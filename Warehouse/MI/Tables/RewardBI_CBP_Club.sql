CREATE TABLE [MI].[RewardBI_CBP_Club] (
    [ClubID]    INT          NOT NULL,
    [BankBrand] VARCHAR (50) NOT NULL,
    CONSTRAINT [PK_MI_RewardBI_CBP_Club] PRIMARY KEY CLUSTERED ([ClubID] ASC)
);

