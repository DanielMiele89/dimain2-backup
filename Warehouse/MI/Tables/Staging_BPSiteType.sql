CREATE TABLE [MI].[Staging_BPSiteType] (
    [Classification] VARCHAR (20) NOT NULL,
    [ClassType]      VARCHAR (4)  NOT NULL,
    CONSTRAINT [PK_MI_Staging_BPSiteType] PRIMARY KEY CLUSTERED ([Classification] ASC)
);

