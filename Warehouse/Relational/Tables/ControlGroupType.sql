CREATE TABLE [Relational].[ControlGroupType] (
    [ControlGroupTypeID] SMALLINT     IDENTITY (0, 1) NOT NULL,
    [Description]        VARCHAR (25) NOT NULL,
    PRIMARY KEY CLUSTERED ([ControlGroupTypeID] ASC)
);

