CREATE TABLE [InsightArchive].[BrandTrackableCheck] (
    [Brand]   VARCHAR (50) NULL,
    [BrandID] SMALLINT     NOT NULL,
    CONSTRAINT [PK_InsightArchive_BrandTrackableCheck] PRIMARY KEY CLUSTERED ([BrandID] ASC)
);

