CREATE TABLE [dbo].[MissingTransactionMethod] (
    [ID]          TINYINT      NOT NULL,
    [Description] VARCHAR (50) NULL,
    CONSTRAINT [PK_MissingTransactionMethod] PRIMARY KEY CLUSTERED ([ID] ASC)
);

