CREATE TABLE [zion].[ZionRequestController] (
    [ID]   INT           IDENTITY (1, 1) NOT NULL,
    [Name] NVARCHAR (50) NOT NULL,
    CONSTRAINT [PK_ZionRequestController] PRIMARY KEY CLUSTERED ([ID] ASC)
);

